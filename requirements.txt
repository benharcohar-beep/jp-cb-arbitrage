numpy>=1.24
pandas>=2.0
tabulate>=0.9
fastapi>=0.100
uvicorn[standard]>=0.20
jinja2>=3.1
httpx>=0.24
yfinance>=1.2
# Refinitiv libraries — only needed for local live runs; safe to omit on Render.
# eikon>=1.1
# refinitiv-data>=1.6
